import 'package:flutter/material.dart';

class BirdsView extends StatelessWidget {
  const BirdsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BirdsView'),
    );
  }
}
