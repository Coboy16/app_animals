import 'package:flutter/material.dart';

class MousesView extends StatelessWidget {
  const MousesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('MousesView'),
    );
  }
}
