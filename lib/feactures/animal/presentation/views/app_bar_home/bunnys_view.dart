import 'package:flutter/material.dart';

class BunnysView extends StatelessWidget {
  const BunnysView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BunnysView'),
    );
  }
}
