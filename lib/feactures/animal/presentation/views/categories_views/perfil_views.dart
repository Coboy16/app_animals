import 'package:flutter/material.dart';

class PerfiView extends StatelessWidget {
  const PerfiView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PerfiView'),
    );
  }
}
