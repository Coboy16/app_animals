import 'package:flutter/material.dart';

class InfoAnimalScreen extends StatelessWidget {
  const InfoAnimalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('InfoAnimalScreen'),
    );
  }
}
