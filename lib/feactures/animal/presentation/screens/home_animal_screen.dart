import 'package:flutter/material.dart';

class HomeAnimalScreen extends StatelessWidget {
  const HomeAnimalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('HomeAnimalScreen'),
      ),
    );
  }
}
